<div class='field'>
  <label
    for='{{name}}'
    class='field__label'
  >{{label}}</label>
  <input
    type='{{type}}'
    class='field__input'
    name='{{name}}'
    placeholder='{{placeholder}}'
    oninput='signup.change(this.name, this.value)'
  />
</div>